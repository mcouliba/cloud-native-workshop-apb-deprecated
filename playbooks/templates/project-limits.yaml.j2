---
apiVersion: v1
kind: LimitRange
metadata:
  name: "{{ project_name }}-core-resource-limits"
  namespace: {{ project_name }}
spec:
  limits:
    - default:
        cpu: 500m
        memory: 1536Mi
      defaultRequest:
        cpu: 100m
        memory: 1024Mi
      max:
        memory: 6Gi
      min:
        memory: 1Mi
      type: Container
    - max:
        memory: 12Gi
      min:
        memory: 6Mi
      type: Pod

